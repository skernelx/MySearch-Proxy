interface:
  display_name: "MySearch"
  short_description: "Install, debug, and use MySearch as the default web/docs/X search path"
  default_prompt: "Use $mysearch to install or debug the MySearch skill/MCP, then use MySearch as the default search path for web, docs, and X. Prefer MySearch over generic web search when it is healthy, and do not invent unsupported modes like hybrid."

policy:
  allow_implicit_invocation: true
