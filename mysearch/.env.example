# MySearch MCP
# 默认优先官方 API；如果你有自己的聚合 API，可覆盖 BASE_URL / PATH / AUTH_*。
# API_KEYS 支持逗号分隔多 key；KEYS_FILE 是可选 fallback。
MYSEARCH_NAME=MySearch
MYSEARCH_TIMEOUT_SECONDS=45

# 最省事的接法：
# 如果你已经有 MySearch Proxy，只填下面两项即可让 Tavily / Firecrawl / Exa
# 默认都走同一套 proxy token；如果 proxy 同时接通了 Social / X，这一套 token
# 也会继续复用到 compatible social 路由。
MYSEARCH_PROXY_BASE_URL=
MYSEARCH_PROXY_API_KEY=

# MCP transport runtime
# 默认通过 stdio 给 Codex / Claude Code 使用。
# 如果你要单独启动 HTTP 入口，可配合：
# python -m mysearch --transport streamable-http --host 127.0.0.1 --port 8000
MYSEARCH_MCP_HOST=127.0.0.1
MYSEARCH_MCP_PORT=8000
MYSEARCH_MCP_MOUNT_PATH=/
MYSEARCH_MCP_SSE_PATH=/sse
MYSEARCH_MCP_STREAMABLE_HTTP_PATH=/mcp
MYSEARCH_MCP_STATELESS_HTTP=false

# Tavily
MYSEARCH_TAVILY_MODE=official

# official 模式：自己维护 Tavily 官方 key 池
MYSEARCH_TAVILY_BASE_URL=https://api.tavily.com
MYSEARCH_TAVILY_SEARCH_PATH=/search
MYSEARCH_TAVILY_EXTRACT_PATH=/extract
MYSEARCH_TAVILY_AUTH_MODE=body
MYSEARCH_TAVILY_AUTH_HEADER=Authorization
MYSEARCH_TAVILY_AUTH_SCHEME=Bearer
MYSEARCH_TAVILY_AUTH_FIELD=api_key
MYSEARCH_TAVILY_API_KEY=
MYSEARCH_TAVILY_API_KEYS=
MYSEARCH_TAVILY_KEYS_FILE=accounts.txt

# gateway 模式：例如 tavily-hikari；建议把 BASE_URL 直接写到 /api/tavily
MYSEARCH_TAVILY_GATEWAY_BASE_URL=
MYSEARCH_TAVILY_GATEWAY_SEARCH_PATH=/search
MYSEARCH_TAVILY_GATEWAY_EXTRACT_PATH=/extract
MYSEARCH_TAVILY_GATEWAY_AUTH_MODE=bearer
MYSEARCH_TAVILY_GATEWAY_AUTH_HEADER=Authorization
MYSEARCH_TAVILY_GATEWAY_AUTH_SCHEME=Bearer
MYSEARCH_TAVILY_GATEWAY_AUTH_FIELD=api_key
MYSEARCH_TAVILY_GATEWAY_TOKEN=
MYSEARCH_TAVILY_GATEWAY_TOKENS=

# Firecrawl
MYSEARCH_FIRECRAWL_BASE_URL=https://api.firecrawl.dev
MYSEARCH_FIRECRAWL_SEARCH_PATH=/v2/search
MYSEARCH_FIRECRAWL_SCRAPE_PATH=/v2/scrape
MYSEARCH_FIRECRAWL_AUTH_MODE=bearer
MYSEARCH_FIRECRAWL_AUTH_HEADER=Authorization
MYSEARCH_FIRECRAWL_AUTH_SCHEME=Bearer
MYSEARCH_FIRECRAWL_AUTH_FIELD=api_key
MYSEARCH_FIRECRAWL_API_KEY=
MYSEARCH_FIRECRAWL_API_KEYS=
MYSEARCH_FIRECRAWL_KEYS_FILE=firecrawl_accounts.txt

# Exa
MYSEARCH_EXA_BASE_URL=https://api.exa.ai
MYSEARCH_EXA_SEARCH_PATH=/search
MYSEARCH_EXA_AUTH_MODE=bearer
MYSEARCH_EXA_AUTH_HEADER=x-api-key
MYSEARCH_EXA_AUTH_SCHEME=
MYSEARCH_EXA_AUTH_FIELD=api_key
MYSEARCH_EXA_API_KEY=
MYSEARCH_EXA_API_KEYS=
MYSEARCH_EXA_KEYS_FILE=exa_accounts.txt

# xAI / X search（可选；不配置时 Web / Docs / Extract 仍可用）
MYSEARCH_XAI_BASE_URL=https://api.x.ai/v1
MYSEARCH_XAI_RESPONSES_PATH=/responses
MYSEARCH_XAI_SEARCH_MODE=official
MYSEARCH_XAI_SOCIAL_BASE_URL=
MYSEARCH_XAI_SOCIAL_SEARCH_PATH=/social/search
MYSEARCH_XAI_AUTH_MODE=bearer
MYSEARCH_XAI_AUTH_HEADER=Authorization
MYSEARCH_XAI_AUTH_SCHEME=Bearer
MYSEARCH_XAI_AUTH_FIELD=api_key
MYSEARCH_XAI_API_KEY=
MYSEARCH_XAI_API_KEYS=
MYSEARCH_XAI_KEYS_FILE=
MYSEARCH_XAI_MODEL=grok-4.20-beta-latest-non-reasoning

# compatible 模式示例：
# 如果没有 grok2api 或官方 xAI key，可以先留空这一整段，MySearch 仍可运行。
# MYSEARCH_XAI_BASE_URL=https://media.example.com/v1
# MYSEARCH_XAI_SOCIAL_BASE_URL=http://127.0.0.1:9875
# MYSEARCH_XAI_SEARCH_MODE=compatible
# MYSEARCH_XAI_API_KEY=your-social-gateway-token

# Tavily gateway 示例：
# MYSEARCH_TAVILY_MODE=gateway
# MYSEARCH_TAVILY_GATEWAY_BASE_URL=http://127.0.0.1:8787/api/tavily
# MYSEARCH_TAVILY_GATEWAY_TOKEN=th-xxxx-xxxxxxxxxxxx

# MySearch 内置 social gateway（可选）
SOCIAL_GATEWAY_UPSTREAM_BASE_URL=https://api.x.ai/v1
SOCIAL_GATEWAY_UPSTREAM_RESPONSES_PATH=/responses
SOCIAL_GATEWAY_UPSTREAM_API_KEY=
SOCIAL_GATEWAY_MODEL=grok-4.1-fast
SOCIAL_GATEWAY_TOKEN=
SOCIAL_GATEWAY_HOST=127.0.0.1
SOCIAL_GATEWAY_PORT=9875
