services:
  mysearch-proxy:
    build: .
    ports:
      - "9874:9874"
    environment:
      - ADMIN_PASSWORD=change-me
      - MYSEARCH_PROXY_DB_PATH=/data/proxy.db
    volumes:
      - ./data:/data
    restart: unless-stopped
